# Product

App / onboarding components ported from the Blueprint kit as they are needed:
agent launcher, chat, checklist, tooltip/coach-mark, the morphing surface, nav,
tables, charts. Built on the kit's conversation engine (BPConvo) + Tabler icons.
Empty until Phase 3 (the Heatmap build) grows this bucket.
